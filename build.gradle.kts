subprojects {
    apply(plugin = "cc.mewcraft.repo-conventions")
    apply(plugin = "cc.mewcraft.java-conventions")

    group = "com.ranull.proxychatbridge"
    version = "1.6"
    description = "Sync server chat across a network"
}